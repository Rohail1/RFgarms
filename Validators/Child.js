/**
 * Created by Rohail on 5/14/2017.
 */

module.exports = function ({joi}) {

  /* const createRoom = (input) => {
   let schema = joi.object().keys({
   name :joi.string().required().error(new Error('Please enter Room Name')),
   roomType: joi.string().required().error(new Error('Please enter a Room Type')),
   floor: joi.string().required().error(new Error('Please enter Room floor')),
   });
   try {
   return joi.validate(input,schema);
   }
   catch (ex){
   return ex;
   }
   };*/

  return  {

  }


};